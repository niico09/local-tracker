{
  "name": "local-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Tailwind CSS build tooling for the local-tracker Go binary.",
  "devDependencies": {
    "@tailwindcss/cli": "4.3.3"
  }
}
